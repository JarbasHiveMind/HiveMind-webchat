# The webchat UI + optional headless bridge.
# The hub itself (hivemind-core) and the skills/agent (ovos-core) run as
# separate services; install and run them independently.
hivemind_webchat
hivemind-bus-client>=0.9.0,<1.0.0
ovos-utils
